apiVersion: v1
kind: Service
metadata:
  name: notification
  namespace: notification
  labels:
    app: notification
spec:
  type: ClusterIP
  ports:
  - name: https
    port: 3000
    targetPort: 3000
  selector:
    app: notification
